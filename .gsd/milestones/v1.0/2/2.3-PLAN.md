---
phase: 2
plan: 3
wave: 2
---

# Plan 2.3: Interactive Price Chart & Dashboard Assembly

## Objective
Integrate an interactive historical price chart using Recharts and assemble all components into a cohesive, responsive dashboard layout.

## Context
- .gsd/SPEC.md
- .gsd/REQUIREMENTS.md (REQ-08, REQ-09, REQ-10)

## Tasks

<task type="auto">
  <name>Create Price Chart Component</name>
  <files>frontend/src/components/PriceChart.jsx, frontend/src/components/PriceChart.css</files>
  <action>
    - Use `recharts` to build a line chart displaying historical prices.
    - The backend `GET /api/stock/{ticker}` or the predict endpoint should ideally return historical close prices. If not, hit `/api/stock/{ticker}`.
    - Style the chart to match the premium dark mode (e.g., glowing grid lines, smooth curves, customized tooltips).
  </action>
  <verify>Verify `PriceChart.jsx` imports and configures a Recharts `<LineChart>`.</verify>
  <done>Chart accurately plots stock history.</done>
</task>

<task type="auto">
  <name>Assemble Main Dashboard Layout</name>
  <files>frontend/src/App.jsx, frontend/src/App.css</files>
  <action>
    - Combine `PredictionForm`, `PredictionResult`, and `PriceChart` in `App.jsx`.
    - Create a responsive CSS grid layout (stacked on mobile, side-by-side or well-spaced on desktop).
    - Add overall polish: dynamic entrance animations (e.g., fade-in), error handling displays (if ticker is invalid).
  </action>
  <verify>Ensure `App.jsx` seamlessly integrates the child components.</verify>
  <done>The full dashboard is visually coherent, responsive, and fully functional.</done>
</task>

## Success Criteria
- [ ] Interactive price chart displays historical trends for the selected ticker.
- [ ] Layout is responsive across desktop and tablet viewpoints.
- [ ] Entire user flow (Input -> Fetch -> View Prediction + Chart) works beautifully and without errors.
