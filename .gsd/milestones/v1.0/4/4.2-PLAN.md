---
phase: 4
plan: 2
wave: 2
---

# Plan 4.2: Frontend Configuration & Polish

## Objective
Abstract the hardcoded API paths into environment variables, ensuring the Vite React application is fully primed for future cloud deployments. Clean up final UX metadata.

## Context
- .gsd/SPEC.md
- frontend/package.json

## Tasks

<task type="auto">
  <name>Extract API URL to Environment Config</name>
  <files>frontend/.env, frontend/src/App.jsx, frontend/src/components/ChatPanel.jsx</files>
  <action>
    - Create `frontend/.env` assigning `VITE_API_URL=http://localhost:8000/api`.
    - Safely sweep and replace `http://localhost:8000/api/...` fetch injections with `` `${import.meta.env.VITE_API_URL || '/api'}/...` `` across `App.jsx` and `ChatPanel.jsx`.
    - Ensure `.env` files are appended appropriately within root / frontend scoped `.gitignore` files mitigating accidental leakages.
  </action>
  <verify>Verify standard paths resolve and fetch queries function uninterrupted.</verify>
  <done>Frontend correctly dynamically aligns to Vite config abstractions.</done>
</task>

<task type="auto">
  <name>Sanity UI Polish</name>
  <files>frontend/index.html, frontend/src/App.css</files>
  <action>
    - Evaluate elements securing inputs gracefully locking via disabled cursors during generalized async states.
    - Confirm the main `index.html` titles cleanly map to the "Stock AI Predictor" brand instead of default scaffolds.
  </action>
  <verify>Verify standard rendering parameters match specs visually.</verify>
  <done>Visual aesthetics perform reliably mapped correctly under edge configurations.</done>
</task>

## Success Criteria
- [ ] No hardcoded API routing literals interact poorly deploying out-of-context.
- [ ] Global aesthetic layout holds standard structure flawlessly securely mapping parameters.
