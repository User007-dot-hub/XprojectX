---
phase: 3
plan: 1
wave: 1
---

# Plan 3.1: Backend Chat API Integration

## Objective
Implement a secure, robust chat endpoint in FastAPI integrating `google-generativeai` to provide financial education exclusively.

## Context
- .gsd/SPEC.md
- backend/app/main.py
- .gsd/phases/3/RESEARCH.md

## Tasks

<task type="auto">
  <name>Implement Chat Service</name>
  <files>backend/app/services/chat_service.py</files>
  <action>
    - Create `chat_service.py`.
    - Setup `google.generativeai` using `settings.GEMINI_API_KEY`.
    - Instantiate model `gemini-1.5-flash`.
    - Provide a system instruction ensuring the bot ONLY answers questions related to finance, stock markets, and investing, acting as a helpful, beginner-friendly tutor. Refuse non-financial prompts politely.
    - Expose an async function `generate_chat_response(messages: list) -> str`.
  </action>
  <verify>Check `chat_service.py` is properly configured.</verify>
  <done>Service securely abstracts Gemini SDK calls.</done>
</task>

<task type="auto">
  <name>Implement Chat Router</name>
  <files>backend/app/routers/chat.py, backend/app/main.py</files>
  <action>
    - Create `chat.py` with `APIRouter`.
    - Define POST endpoint `/api/chat` that accepts a JSON body with `messages` history.
    - Leverage `chat_service` to generate response.
    - Return `{"response": text}`.
    - Handle missing API key cleanly (e.g. 503 error if GEMINI_API_KEY is not set).
    - Uncomment chat router inclusion in `backend/app/main.py`.
  </action>
  <verify>Check `/api/chat` router exists and is integrated into `main.py`.</verify>
  <done>FastAPI serves `/api/chat` correctly.</done>
</task>

## Success Criteria
- [ ] `google-generativeai` SDK connects successfully.
- [ ] Bot enforces finance-only topic boundary.
- [ ] Missing API key is handled gracefully.
