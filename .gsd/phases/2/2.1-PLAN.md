---
phase: 2
plan: 1
wave: 1
---

# Plan 2.1: Initialize React App & Core Layout

## Objective
Initialize the Vite+React application and establish the foundational design system (Vanilla CSS) to ensure a stunning, premium dark-mode aesthetic.

## Context
- .gsd/SPEC.md
- .gsd/REQUIREMENTS.md (REQ-10, NFR-01)
- .gsd/phases/2/RESEARCH.md

## Tasks

<task type="auto">
  <name>Initialize Vite Backend</name>
  <files>frontend/, package.json</files>
  <action>
    - Run `npx -y create-vite@latest frontend --template react`
    - Make sure you run this from the project root `c:\Users\prava\OneDrive\Desktop\XprojectX`.
    - Install `recharts` for charting and `lucide-react` for premium icons: `cd frontend && npm install recharts lucide-react`
  </action>
  <verify>Check `frontend/package.json` for react and lucide-react dependencies.</verify>
  <done>Vite React app is created in `frontend/`.</done>
</task>

<task type="auto">
  <name>Implement Vanilla CSS Design System</name>
  <files>frontend/src/index.css, frontend/src/App.css</files>
  <action>
    - Overwrite `index.css` with a premium dark-mode design system using CSS variables (e.g., `--bg-primary`, `--text-primary`, `--accent-color`).
    - Add utility classes for glassmorphism effects and micro-animations.
    - Set up a base layout in `App.jsx` with a beautiful header containing the app title and an empty main container.
    - Ensure typography uses a modern font (e.g., Inter via Google Fonts in index.html).
  </action>
  <verify>Check `frontend/src/index.css` for CSS variables and animations.</verify>
  <done>Base styling is applied and App.jsx renders without errors with a premium aesthetic.</done>
</task>

## Success Criteria
- [ ] Vite app successfully generated and starts.
- [ ] `index.css` contains a comprehensive set of CSS variables tailored for a premium dark mode.
- [ ] Base layout displays centered header and visually appealing background.
